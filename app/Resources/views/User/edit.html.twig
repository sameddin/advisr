{% extends '::layout.html.twig' %}
{% block content %}

    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">{{ 'homepage'|trans }}</a></li>
        <li><a href="{{ path('user.view', {id: user.id }) }}">{{ user.fullName }}</a></li>
        <li class="active">{{ 'user.profile.edit'|trans }}</li>
    </ol>

    <div class="row">
        <div class="col-md-3">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <i class="fa fa-cog"> {{ 'user.settings'|trans }}</i>
                </div>
                <div class="panel-body">
                    <ul class="nav nav-pills nav-stacked">
                        <li role="presentation" class="active">
                            <a href="{{ path('user.edit') }}">
                                <i class="fa fa-pencil-square-o"> {{ 'user.profile.edit'|trans }}</i>
                            </a>
                        </li>
                        <li role="presentation">
                            <a href="{{ path('user.change_password') }}">
                                <i class="fa fa-lock"> {{ 'user.change_password'|trans }}</i>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="col-md-9">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <i class="fa fa-user"> {{ 'user.profile'|trans }}</i>
                    </div>
                    <div class="panel-body">{{ form(form) }}</div>
                </div>
        </div>
    </div>
{% endblock %}
